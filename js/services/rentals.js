(function(root,factory){var api=factory(root.PMServiceTransport);if(typeof module!=='undefined'&&module.exports)module.exports=factory;if(root)root.PMRentals=api;})(typeof self!=='undefined'?self:this,function(transport){
  'use strict';var esc=transport.escape;
  function fetchRentalListings(opts){opts=opts||{};var qp=['status=eq.active','admin_status=eq.approved','deleted_at=is.null'];var locationEmbed=opts.city?'rental_locations!inner(city,province)':'rental_locations(city,province)';if(opts.city)qp.push('rental_locations.city=ilike.*'+esc(opts.city)+'*');qp.push('select=id,model,year,daily_rate,weekly_rate,monthly_rate,pickup_suburb,company_id,is_available,rental_brands(label),rental_categories(label),rental_vehicle_media(url,is_cover,sort_order),rental_companies(trading_name),'+locationEmbed);qp.push('order='+(opts.order||'created_at.desc'),'limit='+(opts.limit||12));if(opts.offset)qp.push('offset='+opts.offset);return transport.fetchJson('rental_vehicle_listings?'+qp.join('&'));}
  function fetchRentalListingById(id){return transport.fetchJson('rental_vehicle_listings?id=eq.'+esc(id)+'&status=eq.active&admin_status=eq.approved&deleted_at=is.null&select=*,rental_brands(label),rental_categories(label),rental_locations(city,province),rental_vehicle_media(url,is_cover,sort_order),rental_vehicle_specs(*),rental_vehicle_features(feature),rental_companies(business_id,trading_name,rental_phone,rental_whatsapp,rental_email,year_established,deposit_policy,driver_available,cross_border,insurance_included,min_rental_days,avg_rating,review_count,businesses(owner_user_id))').then(function(rows){return rows[0]||null;});}
  return Object.freeze({fetchRentalListings:fetchRentalListings,fetchRentalListingById:fetchRentalListingById});
});
