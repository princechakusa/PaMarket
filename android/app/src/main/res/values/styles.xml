<?xml version="1.0" encoding="utf-8"?>
<resources>

    <style name="AppTheme" parent="Theme.AppCompat.Light.DarkActionBar">
        <item name="colorPrimary">#1A3A8F</item>
        <item name="colorPrimaryDark">#1A3A8F</item>
        <item name="colorAccent">#F5A623</item>
        <item name="android:windowBackground">@drawable/launch_background</item>
    </style>

    <style name="AppTheme.NoActionBar" parent="Theme.AppCompat.NoActionBar">
        <item name="windowActionBar">false</item>
        <item name="windowNoTitle">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowDisablePreview">true</item>
        <!-- Status bar: white (light) icons on the brand navy header/strip, so the
             clock/wifi/battery stay visible in BOTH light and dark mode. -->
        <item name="android:statusBarColor">#1A3A8F</item>
        <item name="android:windowLightStatusBar">false</item>
    </style>

    <style name="AppTheme.NoActionBarLaunch" parent="AppTheme.NoActionBar">
        <!-- Solid blue behind the (transparent) WebView until the HTML splash paints — no black gap -->
        <item name="android:windowBackground">@drawable/launch_background</item>
        <item name="android:windowDisablePreview">true</item>
    </style>

</resources>
